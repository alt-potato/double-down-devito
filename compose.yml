# for local development only!
services:
  frontend:
    build:
      context: ./Project.App/Project.Client
    ports:
      - 8080:3000
    depends_on:
      - backend
  
  backend:
    build:
      context: ./Project.App/Project.Api
    environment:
      # binds to ConnectionStrings:DefaultConnection
      - Server=tcp:250908casinolobby.database.windows.net,1433;Initial Catalog=CasinoLobby;Persist Security Info=False;User ID=bigboss250908;Password=headHoncho!pa$$word;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;
      - WEBSITES_PORT=5131
